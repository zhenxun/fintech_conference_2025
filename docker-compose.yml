version: '3.9'

services:
    website:
        image: docker.io/library/fintech_conference_2023-website:latest
        container_name: fintech_conference_2025_website
        restart: unless-stopped
        tty: true
        environment:
            SERVICE_NAME: fintech_conference_2025_website
        working_dir: /var/www
        ports:
            - "127.0.0.1:9025:9000"
        volumes:
            - ./:/var/www
            - ./php/php.ini:/usr/local/etc/php/php.ini
            # 併發調校：pm.max_children 由預設 5 提高到 20（理由見該檔註解）
            - /Users/fintechmacproserver2/www/.php-fpm/zzz-tuning.conf:/usr/local/etc/php-fpm.d/zzz-tuning.conf:ro
        networks:
            - fintech_highlight_app-network

networks:
  fintech_highlight_app-network:
    driver: bridge
    external: true